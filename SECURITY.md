# 安全说明

## 漏洞反馈

请通过仓库维护者提供的私密渠道报告安全问题；仓库启用 Private vulnerability reporting 后，可通过 GitHub 私密漏洞报告提交。不要在公开 Issue 中粘贴密码、令牌、个人数据或可直接利用的生产细节。

报告应包含受影响版本、复现条件、预期与实际行为，以及经过脱敏的证据。

## 部署边界

- 密码、API 密钥和服务令牌通过运行环境配置，实际 `.env` 文件不提交到仓库。
- JWT 使用签名密钥验证；密码使用 bcrypt 哈希。签名密钥与管理员凭据必须替换开发默认值。
- API、WebSocket、后台任务和文件下载都需要执行资源归属及访问权限检查。
- 生产部署使用 HTTPS，限制 CORS，并将数据库、缓存和执行控制接口置于受控网络。
- 用户代码只允许在通过验收的隔离节点执行，不回退到业务服务器直接执行。
- 数据库、上传文件、模型缓存和生产验收记录不属于源码发布内容。

## 当前限制

当前仍需完善 REST 会话消息的授权闭环、服务端会话撤销和隔离执行文件协议验收。长期记忆、外部连接和通用并行协作不能在缺少作用域检查的情况下启用。

应用与依赖需要持续进行安全更新、权限回归、故障恢复验证及备份恢复演练。已有单元测试或容器健康状态不等价于完整安全验收。
